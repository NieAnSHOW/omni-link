{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and extractor windows",
  "windows": ["main", "extractor-*"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default"
  ]
}
